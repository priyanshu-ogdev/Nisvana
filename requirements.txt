# ==============================================================================
# Project AEGIS — Python Dependencies
# Target: DGX Spark GB10 (Grace Blackwell, CUDA 13)
# ==============================================================================

# Core
numpy>=1.24
scipy>=1.10
soundfile>=0.12
pandas>=2.0
pyarrow>=14.0        # parquet reading (DroneAudioSet fetcher)
requests>=2.31

# Preprocessing
pyloudnorm>=0.1.1    # ITU-R BS.1770-4 LUFS normalization (step3_loudness)

# Fetchers
kaggle>=1.6          # required for mad.py's real audio download (GitHub only hosts annotations)

# Export / industry-standard training format
webdataset>=0.2.90

# ==============================================================================
# ML Training Stack — DGX Spark GB10 / CUDA 13
# ==============================================================================
# PyTorch: install with CUDA 13 index (cu130) for native Grace Blackwell GB10 (sm_121)
# support and alignment with the host CUDA 13 toolchain.
# cu126 is maintained as a fallback for CUDA 12 environments.
--extra-index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.4
torchaudio>=2.4
torchvision>=0.19    # not directly used, but common dependency of training utils

# DeepFilterNet3 — the actual SE backbone (Models 1 & 2).
# Installs pre-built deepfilterlib and df.* module namespace (df.model, df.enhance, df.io).
# NOTE: Avoid [train] extra as it pulls in deepfilterdataloader which requires HDF5 C dev libraries
# and fails to compile; Project AEGIS provides its own native WebDataset / streaming dataloaders.
# deepfilternet is explicitly removed from here to prevent pip from attempting to build
# deepfilterdataloader from source on Python 3.13. It is installed by scripts/00_setup_env.sh instead.

# CleanUMamba — the SE crosscheck backbone (Model 3).
# NOTE: causal-conv1d and mamba-ssm compile CUDA/C++ extensions against your active
# PyTorch installation. Because standard pip build isolation sandboxes the build
# without PyTorch, compiling them directly from requirements.txt triggers:
#   ModuleNotFoundError: No module named 'torch'
# Install without pip sandboxing via:
#   pip install --no-build-isolation -r requirements-mamba.txt
# Or run scripts/00_setup_env.sh which handles this automatically.

# ==============================================================================
# SIH Compliance Evaluation — REQUIRED for valid metrics
# ==============================================================================
# Without these, metrics.py silently falls back to internally-calibrated proxy
# scores instead of the official STOI and PESQ implementations.
pystoi>=0.4.1
pesq>=0.0.4

# ==============================================================================
# Testing & Development
# ==============================================================================
pytest>=8.0

# ONNX export & edge inference
onnx>=1.16
onnxruntime-gpu>=1.18   # GPU-accelerated inference; falls back to CPU provider if no GPU

# Profiling & debugging (optional but recommended on DGX)
tensorboard>=2.17       # training loss/metric visualization
