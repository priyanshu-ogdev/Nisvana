[project]
name = "aegis-backend"
version = "0.1.0"
description = "Project AEGIS — Raspberry Pi 5 Edge AI ANC Backend"
requires-python = ">=3.11"

[project.scripts]
aegis = "src.main:run"

[tool.pytest.ini_options]
testpaths = ["tests"]
